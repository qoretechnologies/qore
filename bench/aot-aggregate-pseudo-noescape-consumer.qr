%modern

int sub main() {
    int rounds = ENV.QORE_BENCH_ROUNDS ? ENV.QORE_BENCH_ROUNDS.toInt() : 20000000;
    list<int> values = (4, 3, 2, 1);
    int total;
    for (int i = 0; i < rounds; ++i) {
        total += aggregateListFirst(values);
    }
    printf("%d\n", total);
    return 0;
}
