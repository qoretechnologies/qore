# Python dependencies for MCP integration tests
#
# The MCP protocol changed shape in revision 2026-07-28, and the two SDK majors that
# implement the two shapes cannot be installed side by side.  Use the era-specific files
# with separate virtual environments instead:
#
#   requirements-modern.txt   mcp 2.x   - protocol 2026-07-28 (stateless)
#   requirements-legacy.txt   mcp 1.x   - protocol 2025-11-25 and earlier (handshake)
#
# run_integration_test.sh creates and manages both automatically.

-r requirements-legacy.txt
