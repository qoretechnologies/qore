# BFL #5423 implementation and qualification

Copyright 2026 Qore Technologies, s.r.o. SPDX-License-Identifier: MIT

Started 2026-09-19. The initial observations below are historical; final local,
installed, authenticated, and live evidence follows. Push, CI, merge, and issue
closure remain separate workflow steps.

## Scope and baseline

The issue body and its sole localization comment were reread using:
`gh issue view 5423 --repo qoretechnologies/qore --json title,body,comments,state,url`.
The main worktree was clean on `feature/app_updates`, HEAD `68158ff96`. The preceding
Stability and Google commits remain ancestors. `git fetch origin` succeeded;
`git log --oneline HEAD..origin/develop` was empty and
`git merge-base --is-ancestor origin/develop HEAD` returned zero. No integration was needed.
No pushes, PRs, CI triggers, account changes, or resource deletions are authorized here.

Required designs and the complete `audit-changes` skill were read before implementation.
The complete final checklist is recorded below and checked before the local commit.

## Credentials and live observations

`~/.profile.d/blf.sh` is absent; `~/.profile.d/bfl.sh` exists. The latter was sourced
only in child shells with tracing disabled. Neither file was printed or modified.
`BFL_APIKEY` is nonempty. A private captured read of
`qrest -j 'connections/blackforestlabs?with_password=true'` verified the expected
name, scheme `bfl`, and equality of its `opts.apikey` with the environment key.
Only these booleans were emitted. An explicitly selected connection is never
silently replaced with another credential.

Initial read-only `GET /v1/credits` calls on `api.bfl.ai`, `api.eu.bfl.ai`, and
`api.us.bfl.ai` all returned HTTP **500** with the supplied credential. Qore's
rebuilt client and an independent Python HTTPS request agreed. The global Qore
response at 2026-09-19 15:38:36 UTC had `text/plain` and `Internal Server Error`.
Intentionally invalid credentials returned HTTP 422, so this is rejection evidence,
not proof of positive authentication. No inference was used as an authentication probe.

At this initial point, credit balance was **unknown**. No paid submissions,
reservations, attributable charges, generated jobs, or new account-owned resources
existed. A 500 response did not establish insufficient credit, and no purchase was
requested on that basis. The user's later top-up and successful checks appear below.

The pricing page stated one credit is USD 0.01. A minimal five-second FLUX 3 draft
was estimated at 30 credits; full HD rendering at 85 credits. The later bounded
representative plan was announced and persisted before the first paid request.
No Stability allowance or balance carried over.

## Contract provenance and reconciliation

Public resources were fetched afresh without authentication into
`/var/tmp/qore-5423-contract/` on 2026-09-19:

|!Resource|!Bytes|!SHA-256|!Operations
|[Main schema](https://api.bfl.ai/openapi.json)|84971|`63d11365e56b138a56f3918ba528a7b0240c8957f48d3c0d55ad908b78969ce1`|29
|[Licenses schema](https://api.bfl.ai/v1/licenses/docs/openapi.json)|1741|`758553faad00cd9bf697532f82d1ed7d95a37f8f04e3a475d80a74455790012b`|1
|[Video edit schema](https://docs.bfl.ai/openapi/video-edit.json)|3650|`92008ad79d7ced2722be954ed66c437c57746beb9df9821927203320364e413c`|1 duplicate

The main hash matches the handoff snapshot exactly. Its coarse version `0.0.1`
does not identify an immutable contract. Neither API schema declares a license;
vendor artifacts must retain attribution and must not be relabeled as Qore MIT code.

The [routing guide](https://docs.bfl.ai/api_integration/integration_guidelines)
requires the returned polling URL for global and regional requests. Delivery hosts
are `delivery.*.bfl.ai`, and signed image URLs expire after ten minutes. API and
delivery trust must remain separate. Redirects must not carry API credentials.

The [LoRA inference guide](https://docs.bfl.ai/flux_2/flux2_lora_inference) documents
six additional Klein fine-tuned endpoints, including KV, BF16, and base variants.
Training and upload are local/Dashboard workflows; no undocumented training or
upload endpoints will be invented. Licensing usage reporting is billing-affecting
and will not be submitted during qualification. Existing fine-tunes will not be deleted.

The [release notes](https://docs.bfl.ai/release-notes) describe the October 2025
legacy finetuning deprecation and the newer separate Klein serving workflow.
The main schema still advertises legacy fine-tune inference and management;
this disagreement must be disclosed rather than silently assuming availability.

Unauthenticated GET route checks returned 405 for `/v1/flux-pro`,
`/v1/flux-pro-1.1`, `/v1/flux-3-video`, `/v1/flux-2-klein-4b-finetuned`, and
`/v1/flux-2-klein-base-9b-finetuned`; `/v1/flux-1.1-pro-video` returned 404.
A 405 establishes a route, not a working inference capability. The older
`flux-pro` alias cannot be declared retired solely because the main schema omits it.

## Initial verification

`cmake --build build --target BflRestClient-qmod BflDataProvider-qmod -j6`
passed before probing and again after the first authentication changes.
Logs: `/var/tmp/qore-5423-initial-build.log` and `/var/tmp/qore-5423-auth-build.log`.
An initial command mistakenly used Qore `-x` instead of `-e`; another omitted the
native i18n module path. Both failed before transport and were corrected.

The first localhost authentication fixture run used rebuilt Release qmods with
`-penable-debug`; seven offline cases passed with **119 assertions** and the
eighth, explicitly opt-in live case skipped. Coverage includes sync, async, raw,
future, repeated option normalization, both connection constructors, blocking and
polling ping, malformed 2xx, 401/402/403/429/500, redirects, rejected-secret echo
redaction, copied credentials, regional endpoints, and unsafe destination rejection.
This is intermediate evidence, not final installed or execution-mode qualification.

## Final implementation and contract boundary

Client/provider versions are 1.1. The app exposes **43 actions**: 37 typed service
operations, five generation/job helpers, and the shared generic API action.
`BflDataProvider.qtest` and `InstalledQualification.q` independently list every
identity. Lightweight expected inventory precedes schema materialization; shared
discovery rejects missing actions even without diagnostics. The `bfl` factory,
connection, media backends, and `BlackForestLabs` app identity remain unchanged.
The compatible image form refreshes to the selected model's actual fields and
constraints. Its output preserves job metadata and adds verified media bytes.

The importer verifies source hashes and reproducibly emits a reviewed contract;
final `python3 tools/bfl-schema-import.py` emits an empty patch. It removes three
invalid zero dimension defaults, namespaces licensing schemas, keeps video-edit as
a documented duplicate, supplements original FLUX Pro from the official SDK, and
adds the six documented LoRA beta variants. Base LoRA exposes only documented
common fields. Training/upload/sharing remain Dashboard/local workflows; no
undocumented model discovery or cancellation is invented. Existing fine-tunes and
licensed usage are never mutated during qualification. Legacy availability remains
an explicit caveat, regardless of its continued inclusion in the main schema.

The official SDK is pinned at commit `c9a6011c2452ce103e0676329617f5f0ad6266a4`,
SHA-256 `837f9d8e8d0eb4928ebe4776544d7ebf53ab058c088489b1fb0b91fa18b9e0dd`.
Its original MIT notice has hash
`514e3da4e34aeb127898a436d94a054ace1470b518e018cf26a870fbf978fe74`;
the LoRA guide has hash
`8e5b1069719fa43a79cb423109290b21cf741572289b2e471ab1e2b80b1da3d6`.
Installed `bfl-provenance.json` records URLs, sizes, hashes, normalizations,
duplicates, and attribution. Vendor schemas are not relabeled as Qore MIT code.
Companion text/legal files have explicit source/AOT installation rules because
the shared resource glob excludes `.txt`.

Authentication retains the immutable selected credential through the shared
sensitive-token owner. Explicit `apikey` takes precedence, including invalid
explicit values. Alternate auth modes and header casing cannot override it.
Production API origins require verified HTTPS; regional copies remain within BFL
and loopback copies stay on the fixture's exact origin. Routed polling validates
one matching encoded ID. Every job owns its poller, sleeper snapshot, last response,
and overall deadline. Actual HTTP fixtures exercise all documented states,
Retry-After, callbacks, malformed Ready responses, concurrency, and deadline paths.

Downloads use a fresh credential-free client, bounded streaming, no redirects,
TLS verification, MIME/signature agreement, and bounded container metadata.
Encrypted draft bundles remain opaque and separately typed. Preflight checks
image/mask dimensions, canvas bounds, video duration/frame metadata, keyframes,
sizes, and model dependencies before transport. Local inputs are capped at 50 MiB;
BFL fetches and validates remote reference content. Fragmented MP4/multiple video
tracks require an external demuxer and are rejected explicitly; metadata parsing
does not claim to decode frames.

Shared fixes remain in their owners: RestSchemaActions provides the base64/file/URL
codec with explicit-null preservation; OpenApi3 retains positional arrays,
response direction, recursion bounds, and native snapshots; RestSchemaValidator
preserves bounds and typed structural uniqueness for unconstrained arrays;
QoreMediaUtils reads bounded MP4 metadata. Regressions cover earlier app consumers.

## Final localization and offline qualification

Fresh recursive extraction has **4993 messages** (404 unique source strings), with
exact root ID/source parity in **cs, de, es, fr, it, ja, ko, pl, sk, uk, zh-Hant,
zh-TW**. All 117 compatible original IDs remain; only the three unsupported
`negative_prompt` messages are removed. `check_catalogs.py` rejects empty or copied
English prose and checks Markdown, URLs, code literals, placeholders, paragraphs,
lists, reviewed terminology, and translated credit-action references. Legitimate
technical model names and ratios can remain identical.

```sh
env QORE_MODULE_DIR=build/qlib-qmod:build/modules/i18n:build/modules/json:build/modules/logger_bin:build/modules/reflection:qlib LD_LIBRARY_PATH=build build/qore bin/qore-data-provider-i18n -m BflDataProvider --owner BflDataProvider -o qlib/BflDataProvider/i18n --check --require-standard-locales --require-complete-locales
python3 examples/test/qlib/BflDataProvider/check_catalogs.py --fresh /var/tmp/qore-5423-i18n-final/data-provider.QmxhY2tGb3Jlc3RMYWJz/root.json
```

Both exit zero with no MISSING/STALE entries. Edited owners/dependencies were
rebuilt in Release and Debug, with no concurrent builds in one directory or
shared/module source edits during builds. Representative commands:

```sh
cmake --build build --target BflDataProvider-qmod ProviderIndex-qmod -j6
cmake --build build-debug --target BflDataProvider-qmod ProviderIndex-qmod -j6
cmake -DQORE_SOURCE_QLIB_DIR=/home/david/src/qore/git/qore/qlib -DQORE_BUILD_QMOD_DIR=/home/david/src/qore/git/qore/build/qlib-qmod -DQORE_QCC_FORMAT_STAMP=/home/david/src/qore/git/qore/build/qcc-format.stamp -P cmake/EnsureSourceQmodSymlinks.cmake
```

Release source symlinks were restored after Debug work. The compiled-only runner
`/var/tmp/qore-5423-modes.py` removes source prepends from copied tests, clears live
flags/selectors, sets matching qmod/native/library paths and `QORE_MODULE_DIR_ONLY=1`,
and verifies 17 actual artifact filenames in five fresh default/AST/IR/JIT/tiered
processes. It runs 20 primary suites in five modes plus 34 broader default-mode
regressions per build: **134 Release and 134 Debug runs pass**, including affected
follow-ups. QUnit reports 876 cases per build, including intentional live-test skips.
Detailed results/log paths are in `/var/tmp/qore-5423-final-offline-results.json`
and `qore-5423-{build,build-debug}-modes-v2` / `-followup` directories.

Final per-mode BFL assertions: Authentication 122 offline, Wire 386, registry/client
13, independent catalog 2327. Shared positional-array tests have 54 assertions,
base64/URL codec 24, and MP4 10. Broader regressions include OpenAPI, schema imports,
envelopes/unions, generic API calls, connection types/schemes, discovery/index/type
graphs, async jobs, and AssemblyAI, AWS, Azure, Google, and Stability app suites.

The required `QORE_TEST_OPTS=-penable-debug ./run_tests.sh -d
qlib/DataProvider/Presentation.qtest`, with rebuilt Release qore/libqore, passes
16 cases/189 assertions. Log: `/var/tmp/qore-5423-presentation-final-run-tests.log`.
Debug presentation also passes 16 cases/189 assertions. These execute shared
source-tree and all-locale qualification, not only BFL-owned comparisons.

Metadata/docs targets pass for BflRestClient, BflDataProvider, OpenApi3,
QoreMediaUtils, RestSchemaActions, and RestSchemaValidator. Final BFL metadata/docs
were rebuilt after the form review. Logs: `/var/tmp/qore-5423-docs-build.log` and
`/var/tmp/qore-5423-final-docs-build.log`. Existing core Doxygen comment warnings
are retained; no affected target failed.

## Fresh installed qualification

`DESTDIR=/var/tmp/qore-5423-stage-x9nhzc_g cmake --install build --prefix /usr`
installed into a fresh empty prefix. The staged qore/libqore and installed-only
paths pass `InstalledQualification.q` in all five fresh execution modes: 43 actions,
zero structured failures, authenticated single-use qualification-token index
publication/readback, missing-action rejection, repeated native serialization,
structured choices/ordinary value objects/positional keyframes/media output types,
recursive root parity, and all twelve translations. Eighteen artifact filenames
are checked. Twenty-two schema/provenance/legal/companion/logo/catalog resources
match source byte-for-byte; eight critical qmods match final Release artifacts.
Evidence: `/var/tmp/qore-5423-install-state.json` and `qore-5423-installed-*.log`.

Explicitly copied external native dependencies, each with source/stage checksum parity:

- `/usr/lib/x86_64-linux-gnu/qore-modules/xml-api-2.0.qmod`: `8979216d27685e60e16c611617ada58186e49649fe2d56b2413868f0cfdaada3`.
- `/usr/lib/x86_64-linux-gnu/qore-modules/yaml-api-2.0.qmod`: `45b37b0a3735c832a4ad4ac208ffddcf89ed685b26bf311938c2b8e513c4c84d`.
- `/usr/lib/x86_64-linux-gnu/qore-modules/msgpack-api-2.0.qmod`: `6bfff1bf50e44d2a414275e414d941492724feea210ed7e8745925492b0a15c5`.

Reproduce with staged `/usr/bin/qore -penable-debug [--exec-mode=ast|ir|jit|tiered]
examples/test/qlib/BflDataProvider/InstalledQualification.q STAGE`; module paths
are staged `usr/lib/x86_64-linux-gnu/qore-modules/3.0.0` and its parent, and the
library path is staged `usr/lib/x86_64-linux-gnu`. No checkout implementation paths.

## Later authentication and credit authorization

The user reported adding USD 10. The next actual Qore credit read returned **1000**
and connection ping succeeded. A correctly formatted deliberately invalid UUID key
returned **403** and failed ping. The opt-in Authentication suite then passed all
eight cases with **126 assertions**, including sync/async authenticated credit reads;
log `/var/tmp/qore-5423-live-auth-qualified.log`. The earlier 422 is only historical
malformed-key evidence. No inference was used as an authentication probe.

A **50-credit (USD 0.50)** maximum was announced for two small images, one image edit,
and one five-second HD draft video after rechecking current pricing. Reservations
are 4, 3, 5, and 30 credits. The persistent locked ledger is
`/home/david/.local/state/qore-5423/credits.json`; each reservation is atomically saved
and fsynced before invoking Qore. The runner privately verifies the named connection
still matches the selected key. Completed requests retain their conservative maximum
against the cap when service cost is absent. Delayed/stale balance reads are recorded
without interpreting a zero or negative delta as a free call. No automatic retry,
automatic recharge, account mutation, or allowance borrowed from another provider.

## Failed-attempt accounting

Corrections were verified without relaxing expectations: action-ID dots became
hyphens; the generic child uses `__call__`; internal ping controls remain declared
while BFL enforces its route; draft MIME uses global `trim`; explicit null precedes
optional codec branches. Relocated tests initially lacked resource paths. Debug
had stale earlier-app artifacts and missing async-connection dependencies. One
presentation run overlapped a binary relink. Corrected paths/rebuilt dependencies
pass. A dynamic-form `addFields` call was caught by a compiler warning and replaced
with supported per-field insertion before final qualification.

The first installed check found omitted `.txt` install rules and a wrong test field
name (`audio` instead of `generate_audio`). Both were corrected and a second fresh
installation passed. The first video live harness included unsupported `seed`;
BflOperation rejected it before transport. An identical offline loopback reproduction
located the unknown-option guard. Its 30-credit reservation was explicitly reconciled
under lock as `preflight-rejected-no-submission` before a separately reserved corrected
attempt. No video POST was sent by that rejected attempt. The exact corrected draft
configuration passes wire fixtures in all Release/Debug modes.


## Complete operation inventory

The 29 main-schema operations, one licensing operation, original FLUX Pro SDK
contract, and six documented LoRA beta contracts are all represented below. The
separate video-edit schema duplicates `start-video-edit`; it is retained as
provenance rather than publishing a second action. Original Pro comes from the
pinned official MIT SDK source; the six LoRA contracts expose documented common
fields and explicitly disclose beta/base-model restrictions.

|!Action|!Method|!Path|!Source
|`get-finetune`|GET|`/v1/finetune_details`|Main schema
|`list-finetunes`|GET|`/v1/my_finetunes`|Main schema
|`delete-finetune`|POST|`/v1/delete_finetune`|Main schema
|`get-credits`|GET|`/v1/credits`|Main schema
|`get-result`|GET|`/v1/get_result`|Main schema
|`start-flux-pro-1-1`|POST|`/v1/flux-pro-1.1`|Main schema
|`start-flux-dev`|POST|`/v1/flux-dev`|Main schema
|`start-flux-pro-1-1-ultra`|POST|`/v1/flux-pro-1.1-ultra`|Main schema
|`start-fill`|POST|`/v1/flux-pro-1.0-fill`|Main schema
|`start-expand`|POST|`/v1/flux-pro-1.0-expand`|Main schema
|`start-finetuned-fill`|POST|`/v1/flux-pro-1.0-fill-finetuned`|Main schema
|`start-finetuned-ultra`|POST|`/v1/flux-pro-1.1-ultra-finetuned`|Main schema
|`start-flux-2-flex`|POST|`/v1/flux-2-flex`|Main schema
|`start-flux-2-klein-9b`|POST|`/v1/flux-2-klein-9b`|Main schema
|`start-flux-2-klein-9b-preview`|POST|`/v1/flux-2-klein-9b-preview`|Main schema
|`start-flux-2-klein-4b`|POST|`/v1/flux-2-klein-4b`|Main schema
|`start-flux-2-max`|POST|`/v1/flux-2-max`|Main schema
|`start-flux-2-pro-preview`|POST|`/v1/flux-2-pro-preview`|Main schema
|`start-flux-2-pro`|POST|`/v1/flux-2-pro`|Main schema
|`start-video`|POST|`/v1/flux-3-video`|Main schema
|`start-video-upscale`|POST|`/v1/flux-tools/video-upscale-v1`|Main schema
|`start-video-edit`|POST|`/v1/flux-tools/video-edit-v1`|Main schema
|`start-outpaint`|POST|`/v1/flux-tools/outpainting-v1`|Main schema
|`start-try-on-v1`|POST|`/v1/flux-tools/vto-v1`|Main schema
|`start-try-on-v2`|POST|`/v1/flux-tools/vto-v2`|Main schema
|`start-erase`|POST|`/v1/flux-tools/erase-v1`|Main schema
|`start-deblur`|POST|`/v1/flux-tools/deblur-v1`|Main schema
|`start-flux-kontext-max`|POST|`/v1/flux-kontext-max`|Main schema
|`start-flux-kontext-pro`|POST|`/v1/flux-kontext-pro`|Main schema
|`report-license-usage`|POST|`/v1/licenses/models/{model_slug}/usage`|Licensing schema
|`start-flux-pro`|POST|`/v1/flux-pro`|Official SDK and generation guide
|`start-lora-4b`|POST|`/v1/flux-2-klein-4b-finetuned`|LoRA inference guide
|`start-lora-9b`|POST|`/v1/flux-2-klein-9b-finetuned`|LoRA inference guide
|`start-lora-9b-kv`|POST|`/v1/flux-2-klein-9b-kv-finetuned`|LoRA inference guide
|`start-lora-9b-kv-bf16`|POST|`/v1/flux-2-klein-9b-kv-bf16-finetuned`|LoRA inference guide
|`start-lora-base-4b`|POST|`/v1/flux-2-klein-base-4b-finetuned`|LoRA inference guide
|`start-lora-base-9b`|POST|`/v1/flux-2-klein-base-9b-finetuned`|LoRA inference guide

Five helpers are `generate-image`, `generate-video`, `get-routed-result`, `wait-for-result`,
and `download-result`. `make-api-call` is injected through the shared generic API
mechanism. Scheme actions use paths only, with no conflicting class registration.
No documented operation is omitted from the discovered contract. Training/upload
remain local or Dashboard workflows: no REST endpoint was invented. Licensing
reports and fine-tune deletion are implemented with billing/deletion caveats but
were deliberately not executed against the account. Legacy model availability and
LoRA account access are not inferred from mock responses or route existence.

## Completed live evidence and accounting

The four serialized paid calls used the production BFL connection/client, actual
routed polling, credential-free signed downloads, and rebuilt Release qmods.
Commands (inside a child shell with tracing disabled and the profile privately
sourced) were:

```sh
python3 examples/test/qlib/BflDataProvider/live-media.py generate-image --run-live
python3 examples/test/qlib/BflDataProvider/live-media.py image-facade --run-live
python3 examples/test/qlib/BflDataProvider/live-media.py edit-image --run-live
python3 examples/test/qlib/BflDataProvider/live-media.py video-facade --run-live --retry-after-preflight-reconcile
python3 examples/test/qlib/BflDataProvider/live-media.py inventory --run-live
python3 examples/test/qlib/BflDataProvider/live-media.py balance --run-live
```

The explicit video retry followed proven local preflight rejection, as recorded
above; it was the first actual video submission. Logs are
`/var/tmp/qore-5423-live-{generate-image,image-facade,edit-image,video-facade-v2}.log`.
All four jobs reached `Ready`, and media was saved before secondary assertions.
The private artifact directory `/var/tmp/qore-5423-live-media` has mode 0700;
media/result files and the ledger have mode 0600. Signed URLs stay in private
result files and are excluded from this audit and the repository.

|!Path|!Model and request|!Job ID|!Decoded output|!Reported cost
|`generate-image`|FLUX Pro 1.1, 512x512, PNG, seed 0|`a5ddea09-5678-47a1-bf14-bbe1a2967a2b`|512x512 RGB PNG|Not reported
|`image-facade`|Klein 4B, 512x512, PNG, seed 0|`9b7b4a1b-81d7-4a5b-836b-92db381e9966`|512x512 RGB PNG|Not reported
|`edit-image`|Klein 4B, original generated image, PNG, seed 0|`a614c9be-f72e-494e-85b5-f2b4095b7c79`|512x512 RGB PNG|Not reported
|`video-facade`|FLUX 3 t2v, 5 seconds, HD draft, audio false|`389c5634-2a48-457c-8d59-3b25ca85dcfd`|1280x704 H.264 MP4, 121 frames, 24 fps, 5.041667 s, no audio|30 credits

Pillow 12.3.0 fully decoded each image. Visual inspection found the requested blue
ceramic mug against white in both generation paths; the edit changed it to bright
red while retaining the shape, handle, composition, and background. These are
visual observations, separate from MIME/signature/dimension/hash checks. Seed zero
was preserved on the submitted wire; BFL returned nonzero seeds, which are retained
as reported rather than rewritten to match the request.

FFmpeg 7.0.2 (imageio-ffmpeg 0.6.0) decoded the entire MP4 with no errors; all 121 RGB
frames were read. Extracted first/middle/end frames show the requested blue mug
rotating on a white turntable. The service's actual HD dimensions and 121/24-second
track duration are preserved. No audio was requested, and no audio track exists.

|!Artifact|!Bytes|!SHA-256|!Reported seed
|`generate-image`|192175|`adfbac9326855bec5a8634ac20cc3a49246f45e369000943aacba1f58e94881b`|2470284641
|`image-facade`|186034|`2886b36b57c3da5a0e0e622e55304074237ffe0f6bc454d50536c86473c607b2`|2128702448
|`edit-image`|232967|`527913edcc2dca1edfc8b3e2f0500084263044da9f76ae2a3dd3f60871756807`|1427879798
|`video-facade`|802315|`edd8a08dcc8b3513ce5d397fdaf64c8921b819ee7939fb646ab5f6d98a8b5f0c`|2761523152

The final installed qore/libqore and installed-only module paths also ran
`LiveMediaArtifacts.q /var/tmp/qore-5423-live-media` without service calls. Each
saved output passed signature/dimension/hash checks and three native serialization
cycles of `BflCompletedJob`, retaining exact bytes, ID, and reported seed. Log:
`/var/tmp/qore-5423-live-installed-artifacts.log`. These are the same eight critical
qmods qualified by the fresh installed sweep; no behavior changed after live calls.

The final read-only fine-tune inventory returned **0**, a valid empty reference list.
No models were trained, uploaded, or deleted. Initial and final authenticated
balances were **1000** and **963.2 credits**: a measured decrease of **36.8 credits
(USD 0.368)**. Video reported a settled cost of **30**; image responses omitted cost.
Their pricing-based estimates are 4, 1.4, and 1.4 credits, totaling 6.8. Thus the
estimated attributed total matches the measured account decrease, but an account
balance delta alone does not prove attribution if other account activity occurs.

Intermediate observations were stale/nonmonotonic (including an unchanged balance
and a later apparent increase), so those individual deltas are not treated as free
calls or refunds. The ledger retains the observations. Conservative completed-call
allowances total **42** against the announced **50-credit** ceiling; **0** credits
remain reserved. There are four completed jobs, one reconciled local rejection,
and **no unresolved submissions**. No further credit purchase is needed.

Temporary delivery URLs expire according to the service's retention policy
(documented image delivery lifetime: ten minutes). There is no documented job
cancel/delete endpoint, so no service-job cleanup is claimed. Saved local artifacts
remain available for review. No account-owned fine-tune or licensing resource was
created by verification.


## Final audit-changes checklist

The complete `/home/david/.codex/skills/audit-changes/SKILL.md` was reread and every
item reviewed against the final staged change. Referenced designs read and applied:
module structure, module sandboxing, cooperative cancellation, data-provider
checklist/development guide, REST schema apps, discovery qualification, semantic
string types, soft lists, generic API actions, provider-index identity, and doc tables.
No C++/parser/JNI changes are involved. The existing #5439/#5448 shared registration
and presentation reports are both required and both pass; neither is replaced by
an app-owned success flag.

|!No.|!Check|!Status|!Evidence
|1|Module catalog|Pass|Existing BflRestClient and BflDataProvider entries in 120_modules.dox.tmpl remain valid.
|2|Core release notes|Pass|900_release_notes.dox.tmpl describes BFL completion and shared schema/media fixes; module notes updated.
|3|CMake module registration|Pass|Directory qore_user_module registration replaces the flat client; all resource install rules verified in a fresh prefix.
|4|QMOD build inventory|Pass|qore_user_module adds the target to the existing generated QMOD inventory; Release/Debug targets and installed AOT modules verified.
|5|Lowercase introductory section|Pass|bflrestclientintro and bfldataproviderintro are the first documentation sections.
|6|Modern module directives|Pass|All edited modules use %modern without redundant legacy directive sets.
|7|Separated class directives|Pass|All separated .qc files scanned outside documentation examples; no parse directives.
|8|Deprecated includes|Pass|No %include in changed Qore sources or tests.
|9|Copyright and attribution|Pass|New authored code, audit, fixture README, and attributes carry 2026; original vendor notice and provenance retained without relicensing.
|10|Directory module layout|Pass|Multi-file client resides in qlib/BflRestClient/BflRestClient.qm.
|11|Duplicate module definition|Pass|Old qlib/BflRestClient.qm is deleted; no second definition.
|12|QPP namespace path|N/A|No QPP/C++ class changes.
|13|Modern test directives|Pass|All seven changed/new qtests and three compiled-only Qore helpers use %modern.
|14|Executable tests|Pass|Qtests and Qore/Python helpers have executable mode.
|15|Test source-path setup|Pass|Ordinary qtests prepend in-repo paths before requires; installed/live artifact helpers intentionally omit prepends and use explicit compiled-only paths.
|16|Dependency loading|Pass|Hard requires are shipped Qore modules; external native dependencies are explicitly staged and checksum-verified.
|17|C++ filesystem sandbox checks|N/A|No C++ changes.
|18|C++ network sandbox checks|N/A|No C++ changes.
|19|QoreSandboxManagerHelper|N/A|No new C++ filesystem/network implementation.
|20|Qore filesystem/network use|Pass|Module File reads load fixed installed schema/logo resources; test File/HTTP fixtures and opt-in media writes are intentional. Production transport uses shared clients/managers.
|21|C++ long-loop cancellation|N/A|No C++ loops changed.
|22|Cancellation API selection|N/A|No C++ cancellation call sites changed.
|23|C++ cancellation frequency|N/A|No C++ loops changed.
|24|C++ blocking cancellation|N/A|No C++ blocking implementation changed; Qore waits reuse the shared cancelable poller/HTTP manager.
|25|Action labels and descriptions|Pass|Independent catalog verifies all 43 registered identities and nonempty metadata; short descriptions are below 80 characters.
|26|Usable action options|Pass|Helpers use getActionOptionFromFields; service actions use shared schema field adaptation. Selected-model forms expose the actual model contract.
|27|Typed action outputs|Pass|37 schema-derived outputs and shared BflCompletedJob/BflDownloadedAsset helper types; repeated native serialization and index readback pass.
|28|API request implementation|Pass|Providers declare supports_request and use doRequestImpl, including strict raw-scalar entry guards and shared request-option validation.
|29|Find-action option parity|N/A|No DPAT_FIND action is added or modified.
|30|Scheme paths without cls|Pass|All app action registrations use bfl paths and no conflicting cls; installed construction passes.
|31|Single-key hash slices|Pass|Changed source review finds no mistaken single-key slice; multi-key slices and explicit field lookup are intentional.
|32|Typed request/response classes|Pass|Handwritten types inherit HashDataType and call addQoreFields in constructors; imported types retain shared schema graphs.
|33|Public input Fields|Pass|BflGenerateImageRequestDataType and BflJobRequestDataType expose public Fields for form registration.
|34|Private output Fields|Pass|Media, draft bundle, sample, and job-response Fields are private with public type constants.
|35|Field metadata|Pass|Reviewed field overlays and recursive presentation cover labels, types, and descriptions; 4993 root messages extracted.
|36|Useful examples|Pass|Prompt, job ID, routed URL, and fine-tune examples included; immutable examples require no service call.
|37|Structured choices|Pass|Model, format, video mode, resolution, creativity, and asset values have human labels; changed labels and ordinary value objects survive serialization/index tests.
|38|Sensitive secrets|Pass|API key and webhook secret are sensitive; actual key scan is clean and echo/header/info diagnostic redaction fixtures pass.
|39|App groups|Pass|AppGroup::AiLlm and AppGroup::DesignCreative used for the app categories.
|40|Private logo resource|Pass|Existing separate SVG loads in Priv; the public BflLogo alias remains for source compatibility. Source/installed bytes match.
|41|App Markdown description|Pass|Business description has capabilities list, website/pricing links, and a billing caveat.
|42|Friendly app name|Pass|Black Forest Labs display name retained; wire identity remains BlackForestLabs.
|43|Plain short descriptions|Pass|Catalog assertions and source review verify concise plain text; technical identifiers remain separate.
|44|Markdown field help|Pass|Reviewed BflPresentation and action/helper descriptions use paragraphs, code literals, lists, and bold caveats; all locales preserve syntax.
|45|Business context|Pass|Descriptions explain product imagery, editing, video, resumable jobs, reference selection, and credit management.
|46|Language literal formatting|Pass|Recursive root scan finds no bare True/False/NOTHING in descriptions.
|47|Field reference formatting|Pass|Technical option references such as id, polling_url, draft_cache, latest, and auto use backticks; labels remain human-readable.
|48|Long description layout|Pass|The sole unique description over 500 characters is the app description, with bold billing heading and capability bullets.
|49|FactoryMap|Pass|Existing bfl to BflDataProvider FactoryMap entry retained and exercised by scheme/discovery tests.
|50|Record-type override signature|N/A|No getRecordTypeImpl override changed or added.
|51|Committed JNI JARs|N/A|No JNI dependencies or JARs.
|52|JAR installation|N/A|No JNI dependencies or JARs.
|53|Lightweight expected inventory|Pass|Exact literal operation/helper identities declared before schema materialization; independent tests and installed missing-action rejection pass.
|54|Structured discovery failures|Pass|Shared discovery reports consumed; no swallowed registration errors. Missing action yields incomplete qualification even without logs.
|55|Authenticated index publication|Pass|Installed check obtains and consumes a current single-use token using the shared locked writer; shared stale/reuse/identity regressions pass.
|56|Versioned canonical schema boundary|Pass|Pinned importer verifies hashes and normalizes once; JSON null/absence/empty/false/zero, tuples, and declared choices tested independently.
|57|Static recursive presentation|Pass|Shared static extraction uses fallback forms, not dynamic model/ref/default/network callbacks; fresh extraction matches shipped root.
|58|Presentation identity stability|Pass|117 of 120 old IDs retained; three unsupported negative_prompt IDs intentionally removed with migration. Nested graph identities and bounded traversal use shared mechanisms.
|59|All-locale parity|Pass|4993 IDs and sources exactly match root in cs/de/es/fr/it/ja/ko/pl/sk/uk/zh-Hant/zh-TW; strict completeness and translation/syntax checks pass.
|60|Fresh installed artifacts|Pass|Empty stage x9nhzc_g; five fresh modes, 18 loaded paths, 22 resource checksums, exact inventory, typed graph/index qualification; saved live media also accepted.
|61|Canonical Java dependency policy|N/A|No Java provider or runtime dependency change.
|62|No workarounds/stubs|Pass|No TODO/FIXME/stub in new production owners. Shared defects fixed in their owners; documented unsupported formats/account gates reject explicitly.
|63|C++ allocation exception safety|N/A|No C++ allocations or xsink changes. Qore exceptions retain safe resumable job identity and ledger reservations on ambiguity.
|64|Thread safety|Pass|Static schema/action/form initialization uses Mutex/AutoLock; each wait/submission gets private timing state/client; sleeper capture locked and concurrent wire waits pass.
|65|Type safety|Pass|Typed callbacks/hashdecls, strict scalar guards, original native schema owners, positional types, and declared finite choices; no new untyped callback shortcut.
|66|Performance|Pass|Unique arrays use structural keys instead of pairwise scans; MP4 parsing bounds box/sample extents; immutable catalogs cached and recursion bounded.
|67|Error handling|Pass|Preflight validates controls/media/model dependencies before billing; terminal/missing/mismatched jobs, deadlines, malformed MIME/bytes, redirects, and size limits covered by HTTP fixtures.
|68|Public documentation|Pass|New API helpers document inputs/returns/failure contracts, module/media examples and caveats; inherited overrides retain parent contracts. Six affected metadata/docs targets pass.
|69|QPP method flags|N/A|No QPP methods changed.
|70|Security|Pass|Parsed destination policy, forced TLS verification, no API redirects, credential-free bounded downloads, safe diagnostics, actual-value credential scan, and signed-token scan pass.
|71|Correctness|Pass|268 final Release/Debug runs; independent contract/fixture assertions; five installed modes; real positive/negative auth; three decoded images and fully decoded video; restored final types accept saved real media.

**Result: 56 Pass, 0 Fail, 15 N/A (71 checks).**

Final QUnit totals: Release 134 runs / 876 cases / 25160 assertions; Debug 134 runs /
876 cases / 25159 assertions. The one-assertion difference is timing-dependent
poll-loop coverage, not a failure. Case totals include explicitly skipped live
cases in the offline sweeps. Actual live authentication is separately recorded
above; paid generation was never repeated across execution modes.

Final `git diff --check` and `git diff --cached --check` pass. Initial staging
flagged terminal blank lines in two exact upstream text snapshots. A two-file
`.gitattributes` exception disables only blank-at-EOF checks on those resources,
preserving their pinned SHA-256 bytes and all other whitespace checks. No vendor
content or runtime artifact changed. Private actual-key scanning found zero
matches, and no signed asset tokens are staged. The scan is repeated after the
final audit is staged. New code/test directive, copyright, and executable-mode
checks pass; only vendor resources retain upstream licensing/formatting.

## Closure readiness

Local acceptance is satisfied: useful complete discovered action coverage,
shared registration/presentation qualification, all twelve locales, authenticated
ping and invalid-key rejection, bounded decoded/inspected live generation/editing/
video, and fresh installed artifacts. There is no remaining credit or capability
blocker for this representative verification. Expensive tools, LoRA beta/account
gates, legacy inference, destructive deletion, and billing usage reports are
contract/wire-qualified, not claimed to have been exercised on this account.

One focused local commit on `feature/app_updates` contains this audited change.
The preceding Stability and Google commits remain intact. No push, PR, pipeline,
merge, or issue-close action was performed. Full CI success for the final revision
and merge remain required before issue closure.
